# Per-pack build artifacts (intermediate iverilog output).
# fixtures/ contents are committed; build/ is regenerated by build.sh.
packs/*/build/

# macOS finder metadata — noise in a repo professors will clone.
.DS_Store
._*
